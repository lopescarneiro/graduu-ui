export { Button, buttonVariants, type ButtonProps } from "./components/button";
export { Badge, badgeVariants, type BadgeProps } from "./components/badge";
export { Card } from "./components/card";
export { Field, Input, inputClasses, type FieldProps, type InputProps } from "./components/field";
// Kit maior (layout/dados) — promovido do graduu-hub (Onda C). Só depende de `cn` + tokens.
export {
  Section,
  Tabs,
  Toggle,
  Sparkline,
  StatCard,
  Select,
  EmptyState,
  Kbd,
  TableShell,
  Avatar,
  Progress,
  thClass,
  tdClass,
} from "./components/kit";
export { cn } from "./lib/cn";
